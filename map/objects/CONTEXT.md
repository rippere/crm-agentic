# objects/ — the nouns

One card per model/table, filed under its cluster folder. Filenames are Title Case matching the class (`Deal.md`, `LeadSegment.md`). Start at `_index.md` for the one-line-per-noun overview, then open the single card you need.

Clusters: `crm-core`, `comms`, `lead-engine`, `sequences`, `intelligence`, `tenancy`. New noun = copy `../_templates/object.md`, do not start blank.
